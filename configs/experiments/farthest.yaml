experiment_name: farthest

data:
  input_jsonl: "data/processed/Input_t5.jsonl"

task:
  use_market: true
  use_news: true
  use_sentiment: true
  use_miq: true

selection:
  cap_per_day: 10
  news_select: "farthest"

checkpoint:
  best_model_path: "outputs/checkpoints/farthest/best_model.pt"